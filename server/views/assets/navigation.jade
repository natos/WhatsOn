#navigation
	#navigation-background
	#navigation-content
		.upc-logo #{config.APP_NAME}
		.menu-item(data-action="TOGGLE-MENU")
			i.icon-reorder
			b.label #{__i('Menu')}
nav.nav
	.menu-item.settings(data-action="TOGGLE-EDIT-MODE")
		i.icon-cog
		b.label #{__i('Settings')}
	.menu-item.login(data-action="LOGIN", title="#{__i('Login')}")
		i.icon-user
		b.label #{__i('Login')}
	a.dashboard(href="/dashboard", title="#{__i('Dashboard')}")
		i.icon-home
		b.label #{__i('Home')}
	a.search(href="/search", title="#{__i('Search')}",)
		i.icon-search
		b.label #{__i('Search')}
	a.nowandnext(href="/nowandnext", title="#{__i('Now &amp; Next')}")
		i.icon-time
		b.label #{__i('Now &amp; Next')}

#user-control
	.menu-item.settings(data-action="SETTINGS", title="#{__i('Settings')}")
		i.icon-cog
		b.label #{__i('Settings')}
	.menu-item.logout(data-action="LOGOUT", title="#{__i('Log Oaut')}")
		i.icon-signout
		b.label #{__i('Log Out')}
		
