.navbar
	.navbar-background
	.navbar-inner
		.upc-logo #{config.APP_NAME}
		.nav
			a.home(href="/", title="#{__i('Home')}")
				i.icon-home
				b.label #{__i('Home')}
			a.movies(href="/movies", title="#{__i('Movies')}")
				i.icon-film
				b.label #{__i('Movies')}
			a.nowandnext(href="/list", title="#{__i('Now &amp; Next')}")
				i.icon-time
				b.label #{__i('Now &amp; Next')}
			a.login(href="/login", title="#{__i('Login')}")
				i.icon-user
				b.label #{__i('Login')}


		.user-control
			a(href="#").active
				i.icon-cogs
				b.label #{__i('Settings')}
			a(href="#")
				i.icon-star
				b.label #{__i('Favorites')}
			a(href="#")
				i.icon-signin
				b.label #{__i('Login')}
			a(href="#")
				i.icon-signout
				b.label #{__i('Logout')}

.search-box
	form(action="/search", method="GET")
		input(id="q", name="q", type="search", results="5", placeholder="#{__i('Search')}")
