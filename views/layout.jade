!!! 5
html
	head(prefix="#{prefix}")
		block head
			title #{title}

			meta(name="viewport", content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no")
			meta(name="apple-mobile-web-app-capable", content="yes")
			meta(names="apple-mobile-web-app-status-bar-style", content="black-translucent")


			if metadata
				each meta in metadata
					meta(property='#{meta.property}', content='#{meta.content}')

			link(href="/css/grid.css", rel="stylesheet")

			script(src="/js/libs/jquery/jquery.js")
			script(src="/js/libs/backbone/underscore.js")
			script(src="/js/libs/backbone/backbone.js")
			script(src="/js/libs/require/require.js", data-main="/js/main")


	body

		#fb-root

		header.main
			a(href="/")
				h1 What's On!
			nav
				ul
					li
						a(href="/") #{__i('Explore TV')}
					li
						a(href="/grid") #{__i('TV Grid')}

			#user-control
				button.fb.btn #{__i('Login')}

			form#search(action="/search")
				.icon
				input(name="q", type="search")


		a#top

	block content